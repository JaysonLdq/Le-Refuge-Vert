{% extends 'base.html.twig' %}

{% block title %}Edit Tarif{% endblock %}

{% block body %}
    <h1>Edit Tarif</h1>

    {{ include('tarif/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_tarif_index') }}" class="btn btn-primary">back to list</a>

    {{ include('tarif/_delete_form.html.twig') }}
{% endblock %}
